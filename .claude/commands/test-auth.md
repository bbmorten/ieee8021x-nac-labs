Run EAP authentication test

Usage: /test-auth [lab-name]

Execute the eapol_test authentication against the RADIUS server in the specified lab.

Examples:
- /test-auth lab1-freeradius-peap (runs PEAP/MSCHAPv2 test)
- /test-auth lab2-freeradius-eaptls (runs EAP-TLS test)
